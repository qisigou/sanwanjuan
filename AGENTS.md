# 项目编程规范

本文件是给 AI 编程助手（Codex 等）阅读和遵守的项目级约定。请始终遵循以下规范。

## 命名规范（重要）

本项目统一使用**中文命名标识符**，包括：

- 变量名：`图书目录`、`当前页数`、`搜索关键词`
- 函数名：`加载_配置文件()`、`创建_应用()`、`查询_图书列表()`
- 类名：`图书管理器`、`阅读进度`（类名习惯首字母大写）
- 文件名/模块名：`搜索.py`、`配置.json`、`类型定义.ts`
- 目录名：`组件/`、`网页/`、`api/`
- SQLite 表名与字段名：同样使用中文，必要时混用字母（如 `图书表`、`书名`、`作者`、`创建时间`，表名也可用 `t_图书` 等便于区分的形式）

### 必须使用英文的例外情况

以下场景不要强行中文化，保持英文：

- 标准库/第三方库的模块名、包名：`import json`、`import os`、`from flask import Flask`
- 框架/工具保留的固定名称：Flask 的 `app.run`、Vite 的 `dev` 脚本、npm 的 `package.json` 字段
- 编程语言关键字：`if`、`for`、`class`、`def` 等
- 与外部系统/协议交互的字段：HTTP 头、JSON API 字段（这些对外接口保持英文，必要时加中文注释；但本项目 SQLite 表名与字段名仍用中文）

## 通用规范

- 所有源代码文件使用 UTF-8 编码
- 缩进统一使用 4 个空格（Python / JavaScript 一致）
- 中文与英文/数字之间留一个空格，例如：`返回 5 本书`、`使用 Python 3`
- 提交信息、注释、文档优先使用中文
- 严禁使用 emoji 符号
- 新代码应与项目现有风格保持一致（参考 `flask/app.py`、`react-frontend/src/` 下的写法）

## 项目结构

- `flask/`：后端（Flask + SQLite，数据库为 `flask/元数据.db`，接口在 `flask/api/`）
- `react-frontend/`：前端（React 19 + Vite + TypeScript，页面在 `src/网页/`，组件在 `src/组件/`）
- 配置文件：`flask/配置.json`（图书目录、封面目录等路径）
